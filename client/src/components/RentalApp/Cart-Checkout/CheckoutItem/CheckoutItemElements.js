import styled from "styled-components";

export const CheckoutItemContainer = styled.div`
  display: flex;
  flex-direction: column;
  margin-right: 3rem;
  margin-bottom: 3rem;
  box-shadow: 0.5rem 0.7rem 0.7rem rgba(0, 0, 0, 0.4);
  border: 1px solid black;
`;
